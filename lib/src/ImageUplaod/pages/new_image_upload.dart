import 'package:flutter/material.dart';

class NewImageUpload extends StatelessWidget {
  const NewImageUpload({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
